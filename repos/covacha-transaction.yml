name: covacha-transaction
product: superpago
type: backend-service
language: python
framework: flask

depends_on:
  code: [covacha-libs]
  services: [cognito, dynamodb, sqs]

consumed_by:
  http: [mf-core, mf-dashboard]

rules:
  testing: "pytest, 80% coverage"
  branch: "develop → main (auto-merge)"
