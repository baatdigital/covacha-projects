name: covacha-inventory
product: superpago
type: backend-service
language: python
framework: flask

depends_on:
  code: [covacha-libs]
  services: [dynamodb]

consumed_by:
  http: [mf-inventory]

rules:
  testing: "pytest, 80% coverage"
  branch: "develop → main (auto-merge)"
